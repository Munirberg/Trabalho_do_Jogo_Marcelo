# Rodando o gerador de imagem

```sudo apt install python3-numpy```

```sudo apt install python3-pil```

```python3 script.py {imagem}```

Irá gerar no terminal todos os rect() corretos para aquela imagem.